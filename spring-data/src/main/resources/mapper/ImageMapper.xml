<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datasource.dao.ImageDao">
  <select id="getImg" resultType="com.datasource.bean.Image">
  select * from p_image where id=#{id}
  </select>
  
  
</mapper>